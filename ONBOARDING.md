## 1. 프로젝트 소개

- **프로젝트명:** 드럼 연주 휴머노이드 로봇 개발
- **한 줄 요약:** **드럼을 연주하면서 사용자와 상호작용하는 휴머노이드 로봇을 개발하는 프로젝트예요.**
- **목표:** 사용자와의 상호작용을 통해 드럼을 연주하거나 행동하는 **로봇 드러머**를 만드는 것이에요.
- **활용:** 주로 원내 로봇 기술 시연용으로 활용되고 있어요 (외부 손님 방문 시 시연, 홍보용 등).

### 현재 진행 상황

현재는 **드럼 연주 기능**만 구현되어 있는 상태예요.

- 드럼 악보를 `txt` 형식 코드로 직접 변환해서 주거나, `MIDI` 파일을 주면 로봇이 읽어서 `txt` 코드로 변환해요.
- 변환된 `txt` 코드를 모션으로 변환해 연주해요.
- 지금까지 로봇은 FSM 기반으로 키보드 입력을 통해서만 제어되고 있어요.

### 앞으로의 개발 방향

1. **사용자 인터랙션 추가**
   - LLM을 사용해 사용자와 대화하는 기능을 붙이고, LLM이 키보드 입력 대신 로봇에게 명령어를 전송하는 시스템을 구현 중이에요.

2. **강화학습 도입**
   - 현재 `txt 코드 → 모션 변환` 방식은 룰베이스라서, 자가 충돌이나 관절 범위 초과 같은 문제가 발생할 여지가 있어요.
   - 문제를 회피하는 알고리즘도 개발했으나 완벽하지 않고 문제가 발생할 때마다 룰이나 악보 코드를 일일이 수정해야 하는 번거로움이 있어요.
   - 그래서 **악보(시간 정보가 포함된 타격 드럼 위치)를 입력으로 받아 모션을 생성하는 정책**을 강화학습으로 학습시켜보려고 시도하고 있어요.

---

## 2. 본인이 맡게 될 업무

**강화학습 기반 모션 생성 정책 학습**이 메인 업무예요.

### 현재 상태

- ✅ **시뮬레이션 환경:** 이미 구현되어 있음
- ⚠️ **보상 함수:** 설계되어 있으나 학습이 잘 수렴하지 않는 상태
- ⚠️ **관측 벡터(State) / 액션(Action):** 변경이 필요할 수 있음

### 예상되는 작업

- 보상 함수 수정/보완 (경우에 따라 재설계)
- 관측 벡터 및 액션 공간 조정
- 시뮬레이션 환경에서 학습 진행 및 결과 분석

> 💡 결국 시뮬레이션 환경 위에서 학습이 잘 되도록 튜닝하고 개선해나가는 역할이에요.

---

## 3. 사용하는 도구

### 개발 스택

- **시뮬레이션:** Isaac Sim / Isaac Lab
- **강화학습 라이브러리:** SKRL (PyTorch 기반)
- **개발 언어:** Python

### 하드웨어 / 개발 환경

```
OS         : Ubuntu 22.04.5
GPU        : RTX 4070 SUPER
CUDA       : 12.8
Driver     : 580.x
Isaac Sim  : 4.5
Isaac Lab  : 2.3.0
Python     : 3.10
PhysX      : GPU (cuda:0)
```

### 협업 도구

지금까지 1인 개발로 진행되어 왔고 앞으로도 그럴 예정이라, **편하신 도구를 사용하시면 돼요.** 참고로 저는 아래를 사용하고 있어요.

- **코드 관리:** GitHub
- **작업 일지:** Notion

---

## 4. 미리 알아두면 좋을 것

> 깊이 공부할 필요는 없고, "아 이런 게 있구나" 정도면 충분해요.

### 강화학습 기본 개념

이 프로젝트의 핵심 방법론이에요. 아래 개념들을 알고 오면 코드를 읽을 때 훨씬 수월해요.

- 에이전트(Agent), 환경(Environment), 상태(State), 행동(Action), 보상(Reward)
- 정책(Policy), 가치 함수(Value Function)
- 에피소드(Episode), 리셋(Reset)
- **PPO (Proximal Policy Optimization)** — 현재 이 프로젝트에서 사용하고 있는 알고리즘이에요. 다만 고정된 건 아니고, 추후 **SAC**나 다른 알고리즘으로 변경하는 것도 고려할 수 있어요.

### Isaac Lab / Isaac Sim이란?

NVIDIA에서 만든 로봇 강화학습 시뮬레이터예요.

- **Isaac Sim:** 물리 시뮬레이터 (로봇, 물체, 중력 등을 시뮬레이션)
- **Isaac Lab:** Isaac Sim 위에서 강화학습 환경을 쉽게 만들 수 있게 해주는 프레임워크
- **GPU 병렬 환경:** 수천 개의 환경을 동시에 돌려서 학습을 빠르게 함

> 설치나 튜토리얼 전체를 따라할 필요는 없고, "어떤 구조인지" 파악하는 정도면 충분해요.

### PyTorch 기초

코드가 전부 PyTorch로 작성되어 있어요. 강화학습 코드를 읽으려면 텐서 연산에 익숙하면 좋아요.

- Tensor: 다차원 배열 (numpy와 비슷)
- GPU 연산: `.to(device)`, `torch.zeros(..., device="cuda")`
- 텐서 shape 다루기: `unsqueeze`, `reshape`, `gather`, `stack`, `cat`
- 브로드캐스팅

### 참고 자료

**Isaac Lab 관련**
- [Isaac Lab 개요](https://isaac-sim.github.io/IsaacLab/main/source/setup/ecosystem.html) — 전체 구조를 파악하기 좋은 시작점
- [Isaac Lab GitHub](https://github.com/isaac-sim/IsaacLab/blob/main/CONTRIBUTORS.md)
- [한국어 강화학습 스터디 가이드](https://wikidocs.net/book/18009)

**SKRL 관련**
- [skrl 공식 문서 — Agents](https://skrl.readthedocs.io/en/latest/#agents) — 이 프로젝트에서 사용하는 RL 라이브러리

### 프로젝트 코드

- **GitHub 레포:** https://github.com/KIST-delight-robotics/DrumRobot-RL

### 도메인 배경 지식

알아두면 좋을 것들이에요.

- **드럼 악보**를 읽을 수 있어야 해요.
- **로봇 기구학 (Kinematics)** — 로봇 팔이 어떻게 움직이는지 이해하는 데 필요해요.
  - **순기구학 (Forward Kinematics):** 관절 각도 → 끝단(팁) 위치 계산
  - **역기구학 (Inverse Kinematics):** 끝단 위치 → 관절 각도 계산
  - 자유도(DOF) 개념
  - 관절 좌표계와 월드 좌표계의 차이

### 로봇 하드웨어 정보

- **전체 자유도:** 총 **13자유도**
  - 허리 1, 오른팔 4, 왼팔 4, 머리 2, 페달 2
- **강화학습에 사용할 관절:** 상체 **9자유도**
- **머리/페달 (4자유도):** 악보에 따라 반응해야 하지만, 강화학습 프레임워크에 포함시킬지 아니면 별도의 간단한 룰베이스로 제어할지(현재 방식) 추후 결정할 예정이에요.